apiVersion: v1
kind: Pod
metadata:
  name: basic-pod
  labels:
    app: security-demo
spec:
  containers:
  - name: demo-container
    image: nginx:1.21
    command: ["/bin/sh"]
    args: ["-c", "sleep 3600"]

